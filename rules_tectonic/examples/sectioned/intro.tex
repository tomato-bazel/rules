\section{Introduction}
\label{sec:intro}

This is the introduction. It exists in its own file
(\texttt{intro.tex}) and is bundled as a \texttt{tex\_section}
target. Sectioning at the build-graph level means editing only
this file triggers no Bazel work outside its own section
target's cache.
