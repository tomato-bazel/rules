load("@bazel_skylib//:bzl_library.bzl", "bzl_library")

package(default_visibility = ["//visibility:public"])

exports_files(["defs.bzl"])

# Public API surface, for stardoc reference-doc generation (//docs).
bzl_library(
    name = "defs_bzl",
    srcs = ["defs.bzl"],
    deps = ["@rules_markdown//markdown:defs_bzl"],
)
