# @generated by rules_gitlab//gitlab:gitlab_ci. DO NOT EDIT —
# edit the gitlab_ci(...) target, then `bazel run <pkg>:<name>.update`.
stages:
  - build
  - test
  - pages
variables:
  GREETING: hello
.setup:
  before_script:
    - echo setting up
build:
  stage: build
  before_script: !reference [.setup, before_script]
  script:
    - echo "$GREETING from build"
pages:
  stage: pages
  script:
    - bazel build //docs:site
    - rm -rf public && mkdir -p public
    - tar -xzf bazel-bin/docs/site.tar.gz -C public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
test:
  stage: test
  script:
    - echo "$GREETING from test"
  coverage: /^TOTAL\s+\d+/
