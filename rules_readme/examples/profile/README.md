<!-- GENERATED by rules_readme. Edit the fragments / this template, then:
       bazel run //examples/profile:readme.write
     CI drift gate: //examples/profile:readme.write_test -->
# Example Group Profile

## Contents

- [Overview](#overview)
- [Repositories](#repositories)

## Overview

This page is generated by `rules_readme` from per-section `markdown_fragment`
targets — the same pattern a GitLab group-profile README graduates onto: one
fragment per section (or per repo), composed into the page with a generated
table of contents and build-time deep links.

## Repositories

Each repository is a row here. As described in the [overview](#overview),
sections are independent fragments, so adding a repo is adding a fragment — and
the link above is checked at build time.

| Repository | Purpose |
|------------|---------|
| `core`     | The foundation library. |
| `app`      | The top-of-DAG application. |
